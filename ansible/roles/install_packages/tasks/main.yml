- name: "Install packages"
  ansible.builtin.apt:
    name:
      - "fzf=0.6*"
      - "shellcheck"
    state: "present"
  become: true
- name: "Ensure .config directory exists"
  ansible.builtin.file:
    path: "~/.config"
    state: directory
    mode: '0755'
